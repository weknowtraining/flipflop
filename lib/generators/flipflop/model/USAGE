Description:
    Generates the Feature database model class for FlipFlop.

Example:
    rails generate flipflop:model

    This will create:
        app/models/feature.rb (class Feature < ActiveRecord::Base)
